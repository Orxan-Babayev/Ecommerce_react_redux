import { Navigation } from "swiper/modules";

export const swiperConfig = {
  style: { "--swiper-navigation-size": "25px" },
  slidesPerView: 4,
  spaceBetween: 10,
  navigation: true,
  modules: [Navigation],
};
